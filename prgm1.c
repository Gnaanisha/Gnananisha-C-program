#include<stdio.h>
int main()
{
    char str1[20],str2[20];
    scanf("%s",str1);
    int len=strlen(str);
    int i=0,temp=len-1;
    while(i<=len-1)
    str2[i]=str[temp];
    i++;
    temp--;
    int b=strcmp(str1,str2);
    if(b==0)
    {
        printf(palindrome);
        else
        {
            printf(" not palindrome");
        }
    }
}

